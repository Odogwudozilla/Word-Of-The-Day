<%= form_with(model: wordbank, local: true) do |form| %>
  <% if wordbank.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wordbank.errors.count, "error") %> prohibited this wordbank from being saved:</h2>

      <ul>
      <% wordbank.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :topic %>
    <%= form.text_area :topic %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
